/**
 * Shared authentication constants used by both the edge proxy and server actions.
 * Keeping them in one place prevents the cookie name from drifting between files.
 */
export const SESSION_COOKIE_NAME = 'session_token';
